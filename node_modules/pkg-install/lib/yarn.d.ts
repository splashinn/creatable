import { InstallConfig, PackageList } from './types';
export declare function constructYarnArguments(packageList: PackageList, config: InstallConfig): string[];
export declare const yarnProjectInstallArgs: string[];
