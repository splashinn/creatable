import { InstallConfig, PackageList } from './types';
export declare function constructNpmArguments(packageList: PackageList, config: InstallConfig): string[];
export declare const npmProjectInstallArgs: string[];
